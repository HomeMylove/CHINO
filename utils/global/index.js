// 收集utils方法
const sendMsg = require('./sendMsg')
const getNickName = require('./getNickName')
const getRandomReply = require('./getRandomEle')
const getUserName = require('./getUserName')

module.exports = {
    sendMsg,
    getNickName,
    getRandomReply,
    getUserName,
}